name: Release

on:
  push:
    tags: ["v*"]

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 25

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Run datagen
        run: ./gradlew :fabric:runDatagen

      - name: Verify committed datagen output is current
        run: |
          if ! git diff --quiet -- 'fabric/src/main/generated/' ':!fabric/src/main/generated/.cache/'; then
            echo "::error::Datagen output is out-of-date. Run ./gradlew :fabric:runDatagen and commit the changes."
            git diff -- 'fabric/src/main/generated/' ':!fabric/src/main/generated/.cache/'
            exit 1
          fi

      - name: Build (compiles + runs tests with generated output in place)
        run: ./gradlew build

      - name: Guard against duplicate Modrinth version
        env:
          MODRINTH_TOKEN: ${{ secrets.MODRINTH_TOKEN }}
        # minotaur uploads unconditionally — it does not check whether this version number
        # already exists, so releasing the same tag twice silently creates a second identical
        # entry. That is exactly how v1.0.0 ended up listed twice. Fail fast instead.
        # NOTE: this also blocks re-running a release whose upload succeeded but whose later
        # steps failed — that is deliberate, since the correct recovery is manual.
        run: |
          TAG=${GITHUB_REF#refs/tags/v}
          existing=$(curl -sf -H "Authorization: $MODRINTH_TOKEN" \
            "https://api.modrinth.com/v2/project/czechcraft/version" \
            | jq -r --arg n "$TAG" '[.[] | select(.version_number == $n)] | length')
          if [ "$existing" != "0" ]; then
            echo "::error::Modrinth already lists $existing version(s) numbered $TAG. Refusing to publish a duplicate — bump mod_version, or archive/remove the existing entry first."
            exit 1
          fi
          echo "No existing Modrinth version numbered $TAG — safe to publish."

      - name: Publish to Modrinth
        env:
          MODRINTH_TOKEN: ${{ secrets.MODRINTH_TOKEN }}
        # `modrinth` uploads the version jar; `modrinthSyncBody` is a separate
        # minotaur task that pushes README.md content to the project body —
        # without it the Modrinth listing description stays empty.
        run: ./gradlew :fabric:modrinth :fabric:modrinthSyncBody

      - name: Verify Modrinth body sync landed
        env:
          MODRINTH_TOKEN: ${{ secrets.MODRINTH_TOKEN }}
        # Fail the release if the body sync silently did nothing — e.g. PAT
        # missing the "Write projects" scope, or minotaur task ordering broke.
        run: |
          body=$(curl -sf -H "Authorization: $MODRINTH_TOKEN" \
            "https://api.modrinth.com/v2/project/czechcraft" \
            | jq -r '.body // ""')
          length=${#body}
          if [ "$length" -lt 100 ]; then
            echo "::error::Modrinth project body is suspiciously short ($length chars). Body sync likely failed — check PAT scopes and minotaur task output above."
            exit 1
          fi
          echo "Modrinth body length: $length chars — sync looks healthy."

      - name: Extract changelog section for this tag
        id: changelog
        run: |
          TAG=${GITHUB_REF#refs/tags/v}
          # Pull the section between "## [TAG]" and the next "## [" header.
          awk -v tag="$TAG" '
            $0 ~ "^## \\[" tag "\\]" { found=1; next }
            found && /^## \[/        { exit }
            found                    { print }
          ' CHANGELOG.md > release-body.md
          echo "body_path=release-body.md" >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          body_path: ${{ steps.changelog.outputs.body_path }}
          files: fabric/build/libs/*.jar

      # Deliberately LAST. Both the v1.0.1 and v1.1.0 releases failed on this step
      # *after* the jar was already public, which skipped GitHub Release creation and
      # needed manual repair each time. Nothing that only inspects already-published
      # state belongs ahead of the steps that finish the release.
      - name: Verify per-version environment metadata on Modrinth
        env:
          MODRINTH_TOKEN: ${{ secrets.MODRINTH_TOKEN }}
        # This used to PATCH environment=client_and_server via the v3 API. That was
        # always redundant: minotaur already uploads with the correct value, proven
        # when the v1.1.0 PATCH failed with an HTTP error yet the field still read
        # back as client_and_server. The write also broke twice against Modrinth's
        # in-flux v3 API, so this now only ASSERTS. If the value is ever wrong, fix
        # it in the Modrinth UI and investigate why minotaur stopped setting it.
        run: |
          TAG=${GITHUB_REF#refs/tags/v}
          VERSION_ID=""
          for attempt in 1 2 3 4 5 6; do
            VERSION_ID=$(curl -sf \
              -H "Authorization: $MODRINTH_TOKEN" \
              "https://api.modrinth.com/v2/project/czechcraft/version" \
              | jq -r --arg n "$TAG" '.[] | select(.version_number == $n) | .id' | head -1)
            if [ -n "$VERSION_ID" ]; then
              echo "Resolved Modrinth version $TAG to $VERSION_ID on attempt $attempt."
              break
            fi
            echo "Version $TAG not visible on the API yet (attempt $attempt/6) — waiting 5s."
            sleep 5
          done
          if [ -z "$VERSION_ID" ]; then
            echo "::error::Could not resolve Modrinth version ID for tag v$TAG after 6 attempts (~25s)."
            exit 1
          fi
          actual=$(curl -sf -H "Authorization: $MODRINTH_TOKEN" \
            "https://api.modrinth.com/v3/version/$VERSION_ID" \
            | jq -r '.environment // ""')
          if [ "$actual" != "client_and_server" ]; then
            echo "::error::Expected environment=client_and_server on Modrinth version $VERSION_ID, got: '$actual'. The jar is already published — fix the field in the Modrinth UI."
            exit 1
          fi
          echo "Environment metadata confirmed: $actual"
